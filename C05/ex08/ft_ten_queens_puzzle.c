#include <stdbool.h>
#include <unistd.h>

bool	is_valid(int position, int c)
{
	int	i;

	i = 0;
	while (i < 10)
	{
		if (position == c)
			return (false);
	}
	// check for diagonals
}

int	ft_ten_queens_puzzle(void)
{
}